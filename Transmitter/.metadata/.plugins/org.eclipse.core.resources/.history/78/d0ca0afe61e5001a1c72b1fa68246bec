#ifndef POTENTIOMETER
#define POTENTIOMETER

class slidePotentiometer{
public:
	uint8_t _pin;
	slidePotentiometer(uint8_t pin = PA3) : _pin(pin){
	  pinMode(pin, INPUT_PULLUP);
	}
	uint16_t getVoltage(){
		analogRead(_pin);
	}
};

#endif 
